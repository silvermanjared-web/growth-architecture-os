# Design System

This folder is intentionally lightweight.

It holds early visual direction and token work for future web or portfolio use. It is not meant to be a full production design system yet.

## Current files

- `tokens.css` — basic color, type, radius, and spacing tokens

## Standard

Keep this layer simple until the portfolio site or visual system needs more structure.
